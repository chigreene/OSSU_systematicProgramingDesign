;; yell-starter.rkt

; 
; PROBLEM:
; 
; DESIGN a function called yell that consumes strings like "hello" 
; and adds "!" to produce strings like "hello!".
; 
; Remember, when we say DESIGN, we mean follow the recipe.
; 
; Leave behind commented out versions of the stub and template.
; 



;sps
;string -> string
;add a "!" to the end of a string
;(define (exclaim n) "") ;stub

(check-expect (exclaim "Help") "Help!")

;(define (exclaim n)  ;template
;  (... n))

(define (exclaim n)
  (string-append n "!"))

(exclaim "Paul")